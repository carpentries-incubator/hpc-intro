Again, if you are working outside the UCL network you will need to connect to Gateway machine 
before accessing Myriad or other local clusters

```
{{ site.local.prompt }} ssh {{ site.remote.user }}@ssh-gateway.ucl.ac.uk
```
{: .language-bash}

Once you are inside the UCL network (either on the UCL Gateway or physically on site), you can SSH into Myriad: